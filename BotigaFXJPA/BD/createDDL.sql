CREATE TABLE Productes (ID VARCHAR(255) NOT NULL, DTYPE VARCHAR(31), FECHA_FINAL DATE, FECHA_INICIO DATE, ID_PROVEEDOR INTEGER, NOM VARCHAR(255), PREU FLOAT, STOCK INTEGER, PRIMARY KEY (ID))
CREATE TABLE Jocs (ID VARCHAR(255) NOT NULL, EDAD_MINIMA INTEGER, PRIMARY KEY (ID))
CREATE TABLE Packs (ID VARCHAR(255) NOT NULL, DESCUENTO FLOAT, PRIMARY KEY (ID))
CREATE TABLE Pack_LISTAJUEGOS (Pack_ID VARCHAR(255), LISTAJUEGOS VARCHAR(255))
ALTER TABLE Jocs ADD CONSTRAINT FK_Jocs_ID FOREIGN KEY (ID) REFERENCES Productes (ID)
ALTER TABLE Packs ADD CONSTRAINT FK_Packs_ID FOREIGN KEY (ID) REFERENCES Productes (ID)
ALTER TABLE Pack_LISTAJUEGOS ADD CONSTRAINT FK_Pack_LISTAJUEGOS_Pack_ID FOREIGN KEY (Pack_ID) REFERENCES Productes (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
